# Formal Parameters and Actual Parameters

    # Formal Parameter = defined in function header (ex. def a (these, things))
    # Actual Parameter = the argument utilized (ex. a (now, this))

def average (a, b):         # a and b formal parameters
    return (a + b) / 2


val1 = 10
val2 = 20

avg = average(val1, val2)   # val1 and val2 actual parameters
print avg
